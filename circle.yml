machine:
  services:
    - docker

dependencies:
  override:
    - docker info

compile:
  override:
    - echo 'none.'

test:
  override:
    - docker build --rm=false .

deployment:
  production:
    branch: master
    commands:
      - bash deploy.sh
